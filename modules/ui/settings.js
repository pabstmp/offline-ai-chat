/* Re-export from the modular settings/ folder. The drawer used to be
   a single 1900-line file; it's now split per-tab under settings/. */
export { initSettings, openSettings, closeSettings, setModelOptions } from "./settings/index.js";
